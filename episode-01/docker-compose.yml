# ============================================================
# Decode AI Using AI — Episode 1
# docker-compose for CLI + optional Jupyter Lab
# ============================================================

services:
  # --- CLI tool: data cleaner + notebook runner ---
  cli:
    build:
      context: ..
      dockerfile: episode-01/Dockerfile
    container_name: decode-ai-cli
    volumes:
      - ./data:/workspace/data
      - ./notebooks:/workspace/notebooks
      - ./logs:/workspace/logs
      - ./src:/workspace/src
      - ../shared:/workspace/shared
    entrypoint: ["python", "-m", "src.cli"]

  # --- Jupyter Lab: for interactive exploration ---
  jupyter:
    build:
      context: ..
      dockerfile: episode-01/Dockerfile
    container_name: decode-ai-jupyter
    ports:
      - "8888:8888"
    volumes:
      - ./data:/workspace/data
      - ./notebooks:/workspace/notebooks
      - ./src:/workspace/src
      - ../shared:/workspace/shared
    entrypoint: []
    command: >
      jupyter lab
      --ip=0.0.0.0
      --port=8888
      --no-browser
      --allow-root
      --NotebookApp.token=decodeai
